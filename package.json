{
  "name": "oggiba-extension-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Shared protocol + tooling for building GDevelop Events Function Extensions, mirroring the good parts of how THNK is built without dragging in its full pipeline for extensions that don't need it.",
  "scripts": {
    "generate": "node scripts/generate-extension.js",
    "build:tier3": "node scripts/build-tier3.js",
    "test": "jest",
    "ts": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "tsup": "^8.0.1",
    "typescript": "^5.4.5"
  }
}
